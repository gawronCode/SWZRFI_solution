@page
@model IndexModel
@{
    ViewData["Title"] = "Conversations";
    ViewData["ActivePage"] = ConversationPanelNavPages.Employees;
    ViewData["ParentLayout"] = this.Layout;
    Layout = "_Layout";
}

<div class="row">
    <div class="col-md-6">
        @*@foreach (var item in Model.)
        {

        }*@
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}